import 'package:flutter/material.dart';

class VariousSubjectsScreen extends StatelessWidget {
  const VariousSubjectsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('موضوعات متنوعة'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class ContemporaryIslamicIssuesScreen extends StatelessWidget {
  const ContemporaryIslamicIssuesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('قضايا اسلاميه معاصرة'),
      ),
    );
  }
}

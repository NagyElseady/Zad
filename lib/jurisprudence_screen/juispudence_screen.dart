import 'package:flutter/material.dart';

class jurisprudenceScreen extends StatelessWidget {
  const jurisprudenceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('الفقة'),
      ),
    );
  }
}

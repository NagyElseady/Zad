import 'package:flutter/material.dart';

class LiteratureEthicsScreen extends StatelessWidget {
  const LiteratureEthicsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('الاداب والاخلاق'),
      ),
    );
  }
}

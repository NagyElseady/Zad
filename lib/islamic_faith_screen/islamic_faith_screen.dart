import 'package:flutter/material.dart';

class IslamicFaithScreen extends StatelessWidget {
  const IslamicFaithScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('العقيدة الاسلامية'),
      ),
    );
  }
}

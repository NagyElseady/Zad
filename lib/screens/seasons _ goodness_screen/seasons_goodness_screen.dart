import 'package:flutter/material.dart';

class SeasonsGoodnessScreen extends StatelessWidget {
  const SeasonsGoodnessScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('مواسم الخير'),
      ),
    );
  }
}
